import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ejercicio-Firebase-Reva',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
